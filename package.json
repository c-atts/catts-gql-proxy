{
  "name": "catts-gql-proxy",
  "description": "Proxy C–ATTS recipe GraphQL queries",
  "version": "0.0.2",
  "private": true,
  "scripts": {
    "deploy": "npx wrangler deploy",
    "dev": "npx wrangler dev --env dev"
  },
  "devDependencies": {
    "wrangler": "^3.53.1"
  }
}
