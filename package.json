{
  "name": "catts-query-proxy",
  "description": "Proxy C–ATTS recipe queries",
  "version": "0.0.3",
  "private": true,
  "scripts": {
    "deploy": "npx wrangler deploy",
    "dev": "npx wrangler dev --env dev"
  },
  "devDependencies": {
    "wrangler": "^3.72.0"
  }
}
